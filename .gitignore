# PyInstaller / local builds (never commit — breaks CI and Windows COLLECT)
build/
dist/
*.spec

# Python
.venv/
venv/
__pycache__/
*.py[cod]

# Compiler staging (if ever copied here)
_compiler_staging_*/
_compiler_prep_backup_*/
.randy_compiler_build_venv/
